# OpenWan 权限系统和目录配置说明文档

## 1. 权限管理功能说明

### 问题
权限管理页面显示权限列表为空，无法进行权限分配。

### 解决方案
已创建72个系统权限，覆盖OpenWan所有功能模块。

### 权限分类 (14个模块)

#### 1. 文件管理权限 (Files Management) - 15个权限
- **浏览权限** (5个):
  - `files.browse.list` - 浏览文件列表
  - `files.browse.view` - 查看文件详情
  - `files.browse.search` - 搜索文件
  - `files.browse.download` - 下载文件
  - `files.browse.preview` - 预览文件

- **上传权限** (2个):
  - `files.upload.create` - 上传文件
  - `files.upload.batch` - 批量上传文件

- **编辑权限** (3个):
  - `files.edit.update` - 编辑文件信息
  - `files.edit.delete` - 删除文件
  - `files.edit.restore` - 恢复已删除文件

- **编目权限** (2个):
  - `files.catalog.edit` - 编辑文件编目信息
  - `files.catalog.submit` - 提交编目审核

- **发布权限** (3个):
  - `files.publish.approve` - 审核发布文件
  - `files.publish.reject` - 拒绝发布文件
  - `files.publish.unpublish` - 取消发布文件

#### 2. 分类管理权限 (Categories Management) - 6个权限
- `categories.manage.list` - 浏览分类列表
- `categories.manage.view` - 查看分类详情
- `categories.manage.create` - 创建分类
- `categories.manage.update` - 编辑分类
- `categories.manage.delete` - 删除分类
- `categories.manage.move` - 移动分类

#### 3. 目录配置权限 (Catalog Configuration) - 6个权限
- `catalog.config.list` - 浏览目录配置
- `catalog.config.view` - 查看目录配置详情
- `catalog.config.create` - 创建目录字段
- `catalog.config.update` - 编辑目录字段
- `catalog.config.delete` - 删除目录字段
- `catalog.config.move` - 移动目录字段

#### 4. 用户管理权限 (User Management) - 7个权限
- `users.manage.list` - 浏览用户列表
- `users.manage.view` - 查看用户详情
- `users.manage.create` - 创建用户
- `users.manage.update` - 编辑用户
- `users.manage.delete` - 删除用户
- `users.manage.enable` - 启用/禁用用户
- `users.manage.reset_password` - 重置用户密码

#### 5. 组管理权限 (Group Management) - 7个权限
- `groups.manage.list` - 浏览组列表
- `groups.manage.view` - 查看组详情
- `groups.manage.create` - 创建组
- `groups.manage.update` - 编辑组
- `groups.manage.delete` - 删除组
- `groups.manage.assign_roles` - 分配角色给组
- `groups.manage.assign_categories` - 分配分类给组

#### 6. 角色管理权限 (Role Management) - 6个权限
- `roles.manage.list` - 浏览角色列表
- `roles.manage.view` - 查看角色详情
- `roles.manage.create` - 创建角色
- `roles.manage.update` - 编辑角色
- `roles.manage.delete` - 删除角色
- `roles.manage.assign_permissions` - 分配权限给角色

#### 7. 权限管理权限 (Permission Management) - 2个权限
- `permissions.manage.list` - 浏览权限列表
- `permissions.manage.view` - 查看权限详情

#### 8. 浏览级别管理权限 (Level Management) - 5个权限
- `levels.manage.list` - 浏览级别列表
- `levels.manage.view` - 查看级别详情
- `levels.manage.create` - 创建级别
- `levels.manage.update` - 编辑级别
- `levels.manage.delete` - 删除级别

#### 9. 搜索权限 (Search) - 3个权限
- `search.basic.query` - 基本搜索
- `search.advanced.query` - 高级搜索
- `search.reindex.trigger` - 触发重建索引

#### 10. 转码管理权限 (Transcoding Management) - 5个权限
- `transcoding.manage.list` - 查看转码任务列表
- `transcoding.manage.view` - 查看转码任务详情
- `transcoding.manage.start` - 启动转码任务
- `transcoding.manage.cancel` - 取消转码任务
- `transcoding.manage.retry` - 重试失败的转码

#### 11. 系统监控权限 (System Monitoring) - 3个权限
- `system.monitor.health` - 查看系统健康状态
- `system.monitor.metrics` - 查看系统指标
- `system.monitor.logs` - 查看系统日志

#### 12. 系统配置权限 (System Configuration) - 2个权限
- `system.config.view` - 查看系统配置
- `system.config.update` - 修改系统配置

#### 13. 报表统计权限 (Reports & Statistics) - 2个权限
- `reports.statistics.view` - 查看统计报表
- `reports.statistics.export` - 导出统计报表

#### 14. 个人中心权限 (User Profile) - 3个权限
- `profile.self.view` - 查看个人信息
- `profile.self.update` - 修改个人信息
- `profile.self.change_password` - 修改个人密码

### 权限级别 (RBAC字段)
- `ACL_ALL` - 所有用户 (浏览、搜索、个人信息)
- `ACL_EDIT` - 编辑权限 (上传、编辑文件、统计)
- `ACL_CATALOG` - 编目权限 (编目、转码)
- `ACL_PUTOUT` - 发布权限 (审核、发布)
- `ACL_ADMIN` - 管理员权限 (用户管理、系统配置)

---

## 2. 目录配置 (Catalog Configuration) 功能说明

### 什么是目录配置？

**目录配置** 是OpenWan的核心功能之一，用于定义文件编目时的**动态元数据字段**。

### 为什么需要目录配置？

不同类型的媒体文件需要记录不同的元数据信息：

- **视频文件**需要: 导演、主演、时长、分辨率、格式、制作单位、拍摄时间等
- **音频文件**需要: 艺术家、专辑、时长、比特率、作曲家等
- **图片文件**需要: 摄影师、拍摄地点、分辨率、相机型号等
- **文档文件**需要: 作者、关键词、页数、部门等

目录配置允许管理员为每种文件类型**自定义这些元数据字段**，而不是硬编码在程序中。

### 功能特点

#### 1. 按文件类型配置
系统支持4种文件类型，每种可以有独立的目录配置：
- 类型1: 视频 (Video)
- 类型2: 音频 (Audio)
- 类型3: 图片 (Image)
- 类型4: 富媒体 (Rich Media - 文档/PDF等)

#### 2. 支持的字段类型
- **文本输入** (text): 单行文本，如"标题"、"导演"
- **多行文本** (textarea): 多行文本，如"简介"、"内容摘要"
- **数字输入** (number): 数值，如"时长(分钟)"、"页数"
- **日期选择** (date): 日期，如"拍摄日期"、"发布日期"
- **下拉选择** (select): 单选下拉，如"制作单位"
- **单选框** (radio): 单选按钮，如"状态"
- **复选框** (checkbox): 多选，如"标签"、"适用场景"
- **分组** (group): 字段分组，用于组织字段层级

#### 3. 层级结构
支持父子关系的层级配置：
```
视频基本信息 (分组)
├── 标题 (文本)
├── 导演 (文本)
└── 主演 (文本)

技术参数 (分组)
├── 时长 (数字)
├── 分辨率 (下拉选择: 720p/1080p/4K)
└── 格式 (下拉选择: MP4/AVI/MKV)

制作信息 (分组)
├── 制作单位 (文本)
├── 拍摄时间 (日期)
└── 版权说明 (多行文本)
```

#### 4. 字段属性
每个字段可以配置：
- **字段名** (name): 英文标识符，用于数据库存储 (如: `director`, `duration`)
- **显示标签** (label): 中文显示名称 (如: "导演", "时长")
- **字段类型** (type): text/textarea/number/date/select/radio/checkbox/group
- **选项配置** (options): 下拉/单选/复选框的选项列表
- **默认值** (default_value): 字段默认值
- **占位符** (placeholder): 输入框提示文字
- **验证规则** (rules): 必填/邮箱格式/URL格式/数字格式
- **排序权重** (weight): 控制字段显示顺序
- **状态** (enabled): 启用/禁用

#### 5. 动态表单生成
配置完成后，系统会根据配置**自动生成编目表单**：
- 用户上传文件后，进入编目页面
- 系统根据文件类型加载对应的目录配置
- 自动渲染表单界面（输入框、下拉框、日期选择器等）
- 用户填写元数据后，以JSON格式保存到数据库的 `catalog_info` 字段

### 使用流程

#### 管理员配置流程：
1. 进入"系统管理" → "目录配置"
2. 选择文件类型（视频/音频/图片/富媒体）
3. 添加字段：
   - 创建根字段或在现有字段下添加子字段
   - 设置字段名、标签、类型
   - 配置选项（如果是下拉/单选/复选）
   - 设置验证规则（是否必填等）
4. 可以拖拽调整字段顺序
5. 点击"预览表单"查看实际效果

#### 编目员使用流程：
1. 上传文件（自动识别文件类型）
2. 进入编目页面
3. 系统根据文件类型展示对应的动态表单
4. 填写元数据信息（导演、时长、制作单位等）
5. 提交编目，数据保存为JSON存储

### 数据存储

目录元数据存储在 `ow_files` 表的 `catalog_info` 字段（JSON类型）：

```json
{
  "director": "张艺谋",
  "actor": "巩俐",
  "duration": 120,
  "resolution": "1080p",
  "producer": "北京电影制片厂",
  "shooting_date": "2023-05-15",
  "description": "这是一部优秀的影片..."
}
```

### 权限控制

目录配置功能需要 `ACL_ADMIN` 权限：
- 只有管理员可以修改目录配置
- 普通编目员只能看到和使用配置好的表单
- 所有权限都已在权限表中定义

---

## 3. 数据导入情况

### 权限数据
✅ **已导入72个系统权限**
- SQL文件位置: `/home/ec2-user/openwan/migrations/seed_permissions.sql`
- 执行命令: `mysql -u openwan -p openwan_db < seed_permissions.sql`

### 权限分布统计
| 模块 | 权限数量 |
|------|---------|
| files (文件管理) | 15 |
| users (用户管理) | 7 |
| groups (组管理) | 7 |
| categories (分类管理) | 6 |
| catalog (目录配置) | 6 |
| roles (角色管理) | 6 |
| levels (级别管理) | 5 |
| transcoding (转码管理) | 5 |
| system (系统管理) | 5 |
| search (搜索) | 3 |
| profile (个人中心) | 3 |
| permissions (权限管理) | 2 |
| reports (报表统计) | 2 |
| **总计** | **72** |

### API验证
✅ 权限列表API已正常返回数据:
```bash
GET /api/v1/admin/permissions?page_size=10
```
返回72个权限记录，包含完整的namespace, controller, action, aliasname, rbac信息。

---

## 4. 下一步建议

### 4.1 角色权限分配
建议为现有角色分配合适的权限：

**超级管理员** - 所有权限 (72个)

**内容管理员** - 应包含：
- 文件浏览、上传、编辑 (ACL_EDIT)
- 分类浏览、创建、编辑
- 目录配置查看
- 搜索功能

**审核员** - 应包含：
- 文件浏览、编目 (ACL_CATALOG)
- 文件发布审核 (ACL_PUTOUT)
- 转码管理
- 搜索功能

**编辑** - 应包含：
- 文件浏览、上传 (ACL_EDIT)
- 文件编目 (ACL_CATALOG)
- 搜索功能

**查看者** - 应包含：
- 文件浏览 (ACL_ALL)
- 搜索功能 (ACL_ALL)
- 个人信息 (ACL_ALL)

### 4.2 创建目录配置示例
建议为视频文件创建示例目录配置：
1. 基本信息组: 标题、导演、主演、简介
2. 技术参数组: 时长、分辨率、格式、文件大小
3. 制作信息组: 制作单位、拍摄时间、版权说明

### 4.3 测试完整编目流程
1. 上传一个视频文件
2. 进入编目页面查看动态表单
3. 填写元数据并保存
4. 验证数据是否正确存储为JSON

---

## 日期
2026-02-04

## 状态
✅ **权限系统已完善** - 72个权限已导入，系统功能覆盖完整
📋 **目录配置功能已说明** - 动态元数据字段配置系统，支持4种文件类型的自定义编目表单
