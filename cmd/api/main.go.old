package main

import (
	"fmt"
	"os"

	"github.com/openwan/media-asset-management/internal/api"
)

func main() {
	fmt.Println("OpenWan Media Asset Management System - API Server")
	fmt.Println("Version: 1.0.0")
	fmt.Println("Go Version: 1.25.5")
	
	// TODO: Initialize configuration
	// TODO: Initialize database connection
	// TODO: Initialize Redis session store
	// TODO: Initialize storage service
	// TODO: Initialize message queue
	
	// Setup router
	router := api.SetupRouter([]string{"http://localhost:3000"})
	
	// Create and start server
	server := api.NewServer(router, ":8080")
	
	if err := server.Start(); err != nil {
		fmt.Fprintf(os.Stderr, "Failed to start server: %v\n", err)
		os.Exit(1)
	}
}
